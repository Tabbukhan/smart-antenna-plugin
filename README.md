# smart-antenna-plugin

This repository contains the sources of the plugin that enables managing smart-antenna devices via velocity gateway.

# Changelog

- v2.4.0:
  - Add logic to allow specific event type to be published to diff topic other than default destination topic
