{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "http://www.speedshield.com/event_joinedWiFiNetwork_v1",
  "description": "Do Not Modify. Generated by Event Schema Generator v1.0.15 on 2020-Jun-15 08:03:33",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "SessionId": {
      "$id": "/properties/SessionId",
      "description": "Units: Count. A lifetime counter incremented on the start of a drive session. This ID uniquely defines a drive session for this telemetry device. The ID may be referenced by other events.",
      "type": "integer",
      "default": 0,
      "minimum": 0.0,
      "maximum": 4294967295.0
    },
    "JoinedWiFiSsid": {
      "$id": "/properties/JoinedWiFiSsid",
      "title": "Wi-Fi network SSID",
      "description": "Units: string name. Specifies the particular Wi-Fi SSID or network name of the WiFi network the device joined, used for the joined wifi network event.",
      "type": "string",
      "default": "",
      "maxLength": 64
    },
    "JoinedWiFiSlot": {
      "$id": "/properties/JoinedWiFiSlot",
      "title": "Wi-Fi Credential Slot",
      "description": "Specifies which credential slot the telemetry device used to join the current WiFi network.",
      "type": "integer",
      "default": 0,
      "oneOf": [
        {
          "title": "Primary",
          "description": "The telemetry device used the primary WiFi credential slot to join the WiFi network.",
          "const": 0
        },
        {
          "title": "Secondary",
          "description": "The telemetry device used the secondary WiFi credential slot to join the WiFi network.",
          "const": 1
        },
        {
          "title": "Stored",
          "description": "The telemetry device used the stored WiFi credential slot to join the WiFi network.",
          "const": 2
        }
      ]
    },
    "JoinedWiFiHashType": {
      "$id": "/properties/JoinedWiFiHashType",
      "title": "Wi-Fi Credential Hash Type",
      "description": "Specifies the method used to hash the credentials used by the telemetry device to join the current Wi-Fi network.",
      "type": "integer",
      "default": 0,
      "oneOf": [
        {
          "title": "Unknown",
          "description": "The telemetry device's credentials used to join the Wi-Fi network are hashed using unknown method.",
          "const": 0
        },
        {
          "title": "CRC-16 CCITT",
          "description": "The telemetry device's credentials used to join the Wi-Fi network are hashed using CRC-16 CCITT.",
          "const": 1
        }
      ]
    },
    "JoinedWiFiHashValue": {
      "$id": "/properties/JoinedWiFiHashValue",
      "title": "Wi-Fi Credential Hash Value",
      "description": "Units: value. A hash of the credentials used by the telemetry device to join the current Wi-Fi network.",
      "type": "integer",
      "default": 0,
      "minimum": 0.0,
      "maximum": 65535.0
    },
    "OperatorId": {
      "$id": "/properties/OperatorId",
      "description": "ID of operator currently logged onto the asset / telemetry device.",
      "type": "integer",
      "default": "",
      "minimum": 0.0,
      "maximum": 4294967295.0
    },
    "FacilityCode": {
      "$id": "/properties/FacilityCode",
      "description": "ID embedded in operator access card, normally referred to as site or facility code. Helps to determine which operator is currently logged onto the asset / telemetry device.",
      "type": "integer",
      "default": "",
      "minimum": 0.0,
      "maximum": 4294967295.0
    }
  },
  "required": [
    "SessionId",
    "JoinedWiFiSsid",
    "JoinedWiFiSlot",
    "JoinedWiFiHashType",
    "JoinedWiFiHashValue"
  ]
}